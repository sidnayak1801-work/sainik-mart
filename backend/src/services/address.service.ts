import { notImplemented } from "../utils/notImplemented";

export const listAddresses = async (_userId: string) => {
  return notImplemented("List addresses");
};

export const createAddress = async (_userId: string, _input: unknown) => {
  return notImplemented("Create address");
};

export const updateAddress = async (_userId: string, _id: string, _input: unknown) => {
  return notImplemented("Update address");
};

export const deleteAddress = async (_userId: string, _id: string) => {
  return notImplemented("Delete address");
};
