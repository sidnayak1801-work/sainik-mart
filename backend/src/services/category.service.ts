import { notImplemented } from "../utils/notImplemented";

export const listCategories = async () => {
  return notImplemented("List categories");
};

export const getCategoryById = async (_id: string) => {
  return notImplemented("Get category");
};

export const createCategory = async (_input: unknown) => {
  return notImplemented("Create category");
};

export const updateCategory = async (_id: string, _input: unknown) => {
  return notImplemented("Update category");
};

export const deleteCategory = async (_id: string) => {
  return notImplemented("Delete category");
};
