import { notImplemented } from "../utils/notImplemented";

export const listProducts = async () => {
  return notImplemented("List products");
};

export const getProductById = async (_id: string) => {
  return notImplemented("Get product");
};

export const createProduct = async (_input: unknown) => {
  return notImplemented("Create product");
};

export const updateProduct = async (_id: string, _input: unknown) => {
  return notImplemented("Update product");
};

export const deleteProduct = async (_id: string) => {
  return notImplemented("Delete product");
};
